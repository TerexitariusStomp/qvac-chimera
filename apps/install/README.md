# apps/install

Installers and installation scripts for Chimera apps.

## Files

- `install-windows.ps1` — Windows PowerShell installer

See `../desktop/`, `../macos/`, and `../mobile*/` for the applications themselves.
