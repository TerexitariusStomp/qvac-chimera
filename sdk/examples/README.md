# sdk/examples

Integration examples for `@chimera/sdk`.

## Examples

- `basic-react/` — Minimal React app with the `useChimera` hook

Each example shows how to add a consent prompt and start/stop mining controls without handling wallets or earnings in your app.
